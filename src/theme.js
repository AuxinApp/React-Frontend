import {createDarkTheme} from 'baseui';


const primitives = {
   

}

const overrides = {
    colors: {
        //placeholder theme in case we want to break out of the out of the box theming 
    }
   
}

const auxinTheme = createDarkTheme(primitives, overrides/* overrides */);


export default auxinTheme;